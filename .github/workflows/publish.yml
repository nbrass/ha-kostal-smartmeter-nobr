name: Publish

on:
  release:
    types:
      - published

jobs:
  release_zip_file:
    name: Build and publish latest release
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: 🛍️ Checkout the repo
        uses: actions/checkout@v6

      - name: 🐍 Setup Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.x"

      - name: 📝 Update manifest version
        run: |
          VERSION="${{ github.event.release.tag_name }}"
          VERSION="${VERSION#v}"  # Remove 'v' prefix if it exists
          sed -i 's/"version": "[^"]*"/"version": "'"$VERSION"'"/' '${{ github.workspace }}/custom_components/ksem/manifest.json'

      - name: 🤐 Create release ZIP
        if: ${{ github.event_name == 'release' }}
        run: |
          cd '${{ github.workspace }}/custom_components'
          zip -r ksem.zip ksem
          mv ksem.zip '${{ github.workspace }}/ksem.zip'

      - name: ☁️ Upload zip to release
        uses: softprops/action-gh-release@v3.0.0
        if: ${{ github.event_name == 'release' }}
        with:
          files: "${{ github.workspace }}/ksem.zip"
